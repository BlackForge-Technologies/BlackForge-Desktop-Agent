# BlackForge-Desktop-Agent
BlackForge AI Desktop Agent: a keyboard-first desktop automator for Ollama. Uses optional screenshots, outputs one JSON action per step, and executes it with approval or auto. Strict mode avoids mouse actions.
